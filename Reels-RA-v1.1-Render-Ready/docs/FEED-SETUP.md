# Feed module setup
Run `supabase/feed-migration.sql` in Supabase SQL Editor after `schema.sql`. The Feed module fetches published reels, uploads video to `reel-media`, writes posts, likes and comments, searches creators and saves the signed-in creator profile.
