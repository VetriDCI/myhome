import{Link,useNavigate}from"react-router-dom";import{useState}from"react";import{supabase}from"../lib/supabase";export default function Login(){const n=useNavigate(),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[e,setE]=useState("");async function go(x){x.preventDefault();if(!supabase)return setE("Add Supabase keys in .env");const{error}=await supabase.auth.signInWithPassword({email,password});if(error)setE(error.message);else n("/feed")}return <main className="auth"><form onSubmit={go}><h1>Reels RA</h1><h2>Log in</h2><input placeholder="Email" type="email" onChange={x=>setEmail(x.target.value)}/><input placeholder="Password" type="password" onChange={x=>setPassword(x.target.value)}/>{e&&<p>{e}</p>}<button>Log in</button><Link to="/register">Create account</Link><Link to="/forgot-password">Forgot password?</Link></form></main>}